class LevelsController < BaseController
	def call
		# "number":selectedLevel.number,
		# "xml":XML(selectedLevel.group).toString(),
		# "desc":selectedLevel.desc,
		# "width":selectedLevel.width,
		# "height":selectedLevel.height,
		# "file":(selectedLevel.file == null || selectedLevel.file == "LevelPack" || selectedLevel.file.length == 0 ? null : selectedLevel.file),
		# "password":console.text
		super
	end
end